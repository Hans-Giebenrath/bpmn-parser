[package]
name = "bpmd-to-svg"
version = "0.1.0"
edition = "2024"

[dependencies]
bpmd-graph = { path = "../bpmd-graph" }
proc_macros = { path = "../proc_macros" }
cosmic-text = {
    version = "0.19",
    default-features = false,
    features = ["std", "swash", "wasm-web"],
}
